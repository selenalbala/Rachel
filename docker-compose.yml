services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: bella
      POSTGRES_PASSWORD: bella
      POSTGRES_DB: bella
    ports:
      - "5432:5432"
    volumes:
      - bella_postgres:/var/lib/postgresql/data
volumes:
  bella_postgres:
